# ============================================================================
# Cattyshack Automations - Environment Variables
# ============================================================================
# Note: Hue and TikTok credentials are configured via the web dashboard
# and stored in the database, not in environment variables.
# ============================================================================

# ----------------------------------------------------------------------------
# Database Configuration (Shared)
# ----------------------------------------------------------------------------
DB_FILE_NAME=file:./data/local.db
NODE_ENV=development

# ----------------------------------------------------------------------------
# Logging Configuration (Event Service)
# ----------------------------------------------------------------------------
LOG_LEVEL=info

# ----------------------------------------------------------------------------
# Service Configuration (Event Service)
# ----------------------------------------------------------------------------
# How often to refresh automations from database (milliseconds, min: 5000)
AUTOMATION_REFRESH_INTERVAL=30000

# How long to wait before retrying stream connection (milliseconds, min: 60000)
STREAM_RETRY_INTERVAL=300000
